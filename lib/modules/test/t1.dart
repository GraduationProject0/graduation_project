import 'package:flutter/material.dart';

class ti extends StatelessWidget {
  const ti({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
      ),
      body: Center(child: Text("T1")),
    );
  }
}
