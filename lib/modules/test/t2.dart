import 'package:flutter/material.dart';

class t2 extends StatelessWidget {
  const t2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
      ),
      body: Center(child: Text("T2")),
    );
  }
}
